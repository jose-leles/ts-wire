{
  "name": "ts-wire",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @ts-wire/core && npm run build -w @ts-wire/errors && npm run build -w @ts-wire/auth && npm run build -w @ts-wire/validate && npm run build -w @ts-wire/cache && npm run build -w @ts-wire/storage && npm run build -w @ts-wire/testing && npm run build -w @ts-wire/example",
    "test": "jest",
    "clean": "npx rimraf packages/*/dist"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/supertest": "^7.2.0",
    "jest": "^30.4.2",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.9"
  }
}
